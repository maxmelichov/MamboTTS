FROM ubuntu:22.04

ENV DEBIAN_FRONTEND=noninteractive
ENV PATH=/root/.cargo/bin:/root/.local/bin:$PATH
ENV APPIMAGE_EXTRACT_AND_RUN=1
ENV NO_STRIP=true

# qemu-user-static is only used when this image runs under emulation, where
# the static-pie linuxdeploy AppImage plugin cannot be executed directly.
RUN apt-get update -qq && apt-get install -y -qq \
    build-essential cmake git curl wget file patchelf ca-certificates \
    qemu-user-static \
    libwebkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev \
    libssl-dev libgtk-3-dev libxdo-dev clang libclang-dev libasound2-dev \
    pkg-config xdg-utils \
 && curl -fsSL https://deb.nodesource.com/setup_22.x | bash - \
 && apt-get install -y -qq nodejs \
 && corepack enable \
 && corepack prepare pnpm@10.15.1 --activate \
 && curl -LsSf https://astral.sh/uv/install.sh | sh \
 && curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y \
 && /root/.cargo/bin/rustup target add x86_64-unknown-linux-gnu \
 && apt-get clean && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace
